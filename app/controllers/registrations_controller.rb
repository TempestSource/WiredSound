class RegistrationsController < ApplicationController
  def new
    # This action just tells Rails to render the signup form
  end

  def create
    # We will build the logic here later to handle the form submission
    # if you decide to handle user creation locally instead of through the API
  end
end