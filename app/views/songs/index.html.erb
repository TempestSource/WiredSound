<h1>Songs</h1>

<div id="songs">
  <%#= debug @songs %>
  <% @songs.each do |song| %>
  <div class="card" style="width: 18rem;">
    <%= link_to song.songName, song_path(song.id) %>
  </div>
  <% end %>
</div>
<%= button_to "Add song", method: :get, class: "btn btn-primary" %>
